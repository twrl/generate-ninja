language: cpp

sudo: false

addons:
  apt:
    sources:
    - sourceline: "ppa:ubuntu-toolchain-r/test"
    packages:
    - gcc-4.9
    - g++-4.9
    - ninja-build

matrix:
  include:
    - os: linux
      env:
        - system="x86_64"


#before_install:
#  - source .travis/setenv.sh

script: make

branches:
  only:
    - master

notifications:
  email: false
